name: .NET

on:
  push:
    branches: [ master ]

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 3.1.301
      - name: get version
        id: version
        uses: notiz-dev/github-action-json-property@release
        with: 
          path: 'plugin.json'
          prop_path: 'Version'
      - run: echo ${{steps.version.outputs.prop}} 
      - name: Build
        run: |
          release_name='WindowWalker-${{steps.version.outputs.prop}}'
          dotnet publish 'Microsoft.Plugin.WindowWalker.csproj' --framework netcoreapp3.1  -c Release -o "$release_name"
          7z a -tzip "${release_name}.zip" "./${release_name}/*"
          rm -r "$release_name"
      - name: Publish
        uses: softprops/action-gh-release@v1
        with:
          files: "WindowWalker*"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
